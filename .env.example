# Copy this file to .env.local and fill the real secrets. Do NOT commit .env.local to git.
# Vite requires env vars exposed to the client to start with VITE_

VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
VITE_GOOGLE_CLIENT_SECRET=your-google-client-secret
VITE_GEMINI_API_KEY=your-gemini-api-key
VITE_WEATHER_API_KEY=your-weather-api-key
VITE_WEBHOOK_URL=https://agentx2026.app.n8n.cloud/webhook/time-assistant

# Usage:
# 1. Copy to .env.local
# 2. Replace values with your real keys
# 3. Restart the dev server
# 4. Do NOT commit .env.local
